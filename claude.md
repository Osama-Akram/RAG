["str, Any]` for dictionary parameters\n\n6. **No implicit `Any`**: Use explicit `Any` type only when truly necessary\n\n7. **Use typing module**: Import types from `typing` module (List, Dict, Optional, Tuple, etc.)\n\n8. **Return Type for Generators**: Use `Iterator[T]` or `Generator[Yield, Send, Return]`\n\n9. **Callable Type Hints**: Use `Callable[[ParamTypes], ReturnType]` for function parameters\n\n### Examples\n\n```python\n# \u274c BAD - No type hints\ndef calculate(x, y):\n    return x + y\n\n# \u2705 GOOD - Complete type hints\ndef calculate(x: int, y: int) -> int:\n    return x + y\n\n# \u274c BAD - Missing return type\ndef process_data(items):\n    return [item * 2 for item in items]\n\n# \u2705 GOOD - Full type hints\ndef process_data(items: List[int]) -> List[int]:\n    return [item * 2 for item in items]\n\n# \u274c BAD - Missing None return\nclass DataProcessor:\n    def __init__(self, config):\n        self.config = config\n\n# \u2705 GOOD - With None return\nfrom typing import Dict, Any\n\nclass DataProcessor:\n    def __init__(self, config: Dict[str, Any]) -> None:\n        self.config = config\n\n# \u274c BAD - Implicit Any\ndef load_data(path):\n    with open(path) as f:\n        return json.load(f)\n\n# \u2705 GOOD - Explicit types\nfrom typing import Any\n\ndef load_data(path: str) -> Dict[str, Any]:\n    with open(path) as f:\n        return json.load(f)\n\n# \u2705 GOOD - Dataclass with types\nfrom dataclasses import dataclass\n\n@dataclass\nclass Document:\n    text: str\n    source: str\n    metadata: Dict[str, Any] = field(default_factory=dict)\n```\n\n### Type Hint Imports to Use\n\n```python\nfrom typing import (\n    Any,\n    Callable,\n    Dict,\n    Generator,\n    Iterable,\n    Iterator,\n    List,\n    Literal,\n    Optional,\n    Sequence,\n    Tuple,\n    TypedDict,\n    Union,\n)\nfrom dataclasses import dataclass, field\n```\n\n### Common Patterns\n\n```python\n# Optional parameters\ndef process(value: Optional[str]) -> str:\n    return value or \"default", "Union types\ndef handle_input(data: Union[str, int, float]) -> float:\n    return float(data)\n\n# Callable type hint\ndef apply_function(func: Callable[[int], int], value: int) -> int:\n    return func(value)\n\n# Iterator/Generator\ndef stream_items() -> Iterator[str]:\n    for item in items:\n        yield item\n\n# TypedDict\nclass DocumentMetadata(TypedDict):\n    source: str\n    timestamp: str\n    tags: List[str]\n\ndef add_metadata(doc: str, meta: DocumentMetadata) -> Dict[str, Any]:\n    return {\"text", "doc, **meta}\n```\n\n### Enforcement\n\nWhen generating or modifying Python code:\n- ALWAYS add type hints\n- NEVER skip return type annotation\n- NEVER use implicit `Any`\n- PREFER `list[str]` over `List[str]` (Python 3.9+ syntax)\n- PREFER `dict[str, Any]` over `Dict[str, Any]` (Python 3.9+ syntax)"]